# Release status

Current status: **Release-grade** — the `E2E` notebook blob `2e28896c` (committed at `0edb5de`) executed top-to-bottom in a clean Kaggle Tesla T4 runtime on 2026-09-26 (11/11 code cells ok, 1 restart after the install cell, 1292.2 s, 205 files / 1248 MB staged from the Hub into an empty cache), recorded in `docs/release-verification.md`; held-out test (164 widgets, one seeded split) CIDEr-D frozen 1.318 → adapted 1.356 (`large-widget` 2.246 → 2.291, `small-widget` 0.934 → 0.969; constant-caption baseline 0.138), BLEU-4 0.365 → 0.232, best validation epoch 3 of 3; the exported adapter reloaded with 8/8 identical captions. An earlier T4 attempt on `fe3385a` failed at the staging cell on a `_hub_download` name collision between the carried `samples.py` and `pipeline.py` cells, fixed in `6583937` and regenerated in `0edb5de`. The fine-tuning recipe (`LEARNING_RATE = 1e-5`, three epochs, two decoder blocks, batch 4) moved CIDEr-D, ROUGE-L and unigram F1 up by small margins (+0.038, +0.003, +0.001) while BLEU-4 fell, on one seed, so the notebook records `adapted_beats_frozen` rather than asserting it. At that revision the pipeline package with its adaptation contract, the carried `metrics.py` and `samples.py` (one digest-pinned Widget Captioning test shard from `bevaya/RICO-WidgetCaptioning`, CC BY 4.0, split by app), the offline and model-backed suites, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), the static validator, the generator parity checks and the CI workflow exist and are green. Any later change to the carried modules or to the notebook yields a new blob and returns the registry to **Candidate** until a clean run of that blob is recorded.
