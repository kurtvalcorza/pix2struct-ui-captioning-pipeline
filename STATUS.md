# Release status

Current status: **Candidate — `E2E` tutorial source complete, corpus pin and execution outstanding**. The pipeline package with its adaptation contract (`predict`, `evaluate`, `adapt`, `save_artifact`, `load_artifact` / `from_artifact`), the carried `metrics.py` (BLEU-4, ROUGE-L, CIDEr-D, the constant-caption baseline and a colour-neighbour baseline over the widget crop) and `samples.py` (one Widget Captioning test shard from `bevaya/RICO-WidgetCaptioning`, CC BY 4.0, split by app), the offline unit suites (pipeline, adaptation, role-helper, import-boundary and notebook-parity) and the model-backed adaptation suite on a small random Pix2Struct built offline from the committed config, processor and tokenizer, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/pix2struct_ui_captioning_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR4). Outstanding before any run: the shard's SHA-256 pin (recorded by `tools/pin_corpus.py`, which needs Hub access; the sample reader refuses to run until it is recorded). The fine-tuning recipe has not been run on the real checkpoint, so the notebook records `adapted_beats_frozen` instead of asserting it. The Kaggle and local runs in `docs/release-verification.md` were of the previous `TASK-INFERENCE` notebook and are not evidence for this revision; the repository is not release-grade until a Colab/Kaggle execution for the exact candidate blob is recorded there.
